app = "voicetudu-web"
primary_region = "mia" # Miami - cercano a Colombia y Latinoamérica

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 1

[[http_service.checks]]
  grace_period = "10s"
  interval = "30s"
  method = "GET"
  timeout = "5s"
  path = "/health"

[compute]
  size = "shared-cpu-1x"
  memory = "256mb"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"

# Scaling configuration
[deploy]
  strategy = "rolling"
  max_unavailable = 0
